# 项目工作约定

## 版本管理

每次准备版本更新前，必须读取并遵守 `docs/VERSIONING.md`，检查 `apps/client/pubspec.yaml` 和 `CHANGELOG.md` 中的当前版本。

用户指定的规则：补丁更新递增最后一位；功能更新递增中间一位；大幅度 UI 修改或模块调整递增第一位。递增较高位后，将较低位归零；混合变更取最高级别。同一轮更新中的修正或重新构建不重复递增对外版本。当前登记版本为 0.3.0，后续以版本配置和更新记录为准。

不要把未重新构建的旧安装包描述为新版本，不得把测试版登记描述为正式发布。

## 更新日志

每次更新都必须维护仓库根目录 `CHANGELOG.md`，不能只改版本号。按版本由新到旧记录版本号、日期、变更类型，并写明新增、调整、修复、验证结果和已知限制；无对应内容的分类省略。条目描述具体行为与用户影响，不能只写“优化体验”“修复问题”。

同一轮版本中的补充说明或修正合并到该版本条目，不重复创建版本、不因仅补写日志而递增版本。保留历史记录，不编造旧版本的日期、功能或验证结果；未实现的平台和未完成的验收必须明确标注。
