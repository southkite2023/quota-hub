# 项目工作约定

## 版本管理

每次准备版本更新前，必须读取并遵守 `docs/VERSIONING.md`，检查 `apps/client/pubspec.yaml` 和 `CHANGELOG.md` 中的当前版本。

用户指定的规则：补丁更新递增最后一位；功能更新递增中间一位；大幅度 UI 修改或模块调整递增第一位。递增较高位后，将较低位归零；混合变更取最高级别。同一轮更新中的修正或重新构建不重复递增对外版本。当前登记版本为 0.2.1，后续以版本配置和更新记录为准。

不要把未重新构建的旧安装包描述为新版本，不得把测试版登记描述为正式发布。
