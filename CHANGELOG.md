# 更新日志

按版本由新到旧记录，每个版本说明具体变更与用户可见影响。仅保留实际发生的条目，未实现功能列入已知限制。

## 0.3.0 — 2026-09-25

类型：功能更新。状态：已通过 GitHub Releases 发布测试版，尚未完成正式版验收。客户端配置：`0.3.0+4`。

### 新增

- 阿里云中国站云账户查询：配置 AccessKey ID / Secret，分别显示可用额度与现金余额；币种以接口返回为准。
- 腾讯云中国站云账户查询：配置 SecretId / SecretKey，显示可用、现金、赠送余额；将接口返回的分精确换算为人民币元。
- 云账户支持多账户并存、独立刷新、编辑、删除、桌面组件选择与金额隐藏。
- 云凭据沿用 Android Keystore 加密存储；Secret 只参与本机签名，固定官方查询地址，禁止跟随重定向。

### 调整与修复

- 账户入口统一称为“余额账户”，同时容纳模型平台和云平台。
- 原百炼待适配位置改为阿里云云账户入口，明确显示整个费用账户余额，不将其称作百炼专属额度。
- 编辑云账户时，密钥 ID 变化必须重新输入 Secret；已有模型账户配置继续兼容。
- 区分缺失余额、真实零余额、负余额与查询失败；桌面组件支持阿里云可用额度。

### 分发

- 将已验证的 `0.3.0+4` Android APK 发布到 [GitHub Releases v0.3.0](https://github.com/southkite2023/quota-hub/releases/tag/v0.3.0)，设为最新版本，提供 APK 直接下载和 SHA-256 校验文件。
- 本次沿用原测试包，不改变应用版本、不重新签名；Releases 标题及说明明确标注测试构建。

### 验证与限制

- Flutter 静态检查通过，42 项客户端测试通过，Web 构建通过；本地合约与服务端 17 项测试通过。新增测试覆盖签名固定样例、精确金额、错误响应、凭据不进入快照、密钥 ID 变更及旧配置兼容。
- Android APK 与 Windows 构建通过；验证源码提交 `3f40723ef07060a363a7b34535f899239522b440`。后续仅补记此日志，不改变安装包代码。
- [Android 0.3.0+4 测试包](https://github.com/southkite2023/quota-hub/suites/97631050216/artifacts/10831186221)；[客户端验证](https://github.com/southkite2023/quota-hub/actions/runs/36052195661)、[原生构建](https://github.com/southkite2023/quota-hub/actions/runs/36052195648)。下载需登录 GitHub，构建产物保留至 2026-10-02 04:06（北京时间）。
- 尚未使用真实云账户或 Android 真机验收。本轮支持阿里云、腾讯云中国站长期子账户密钥；暂不支持临时 STS 凭据和国际站。
- 华为云、AWS、Azure 等平台尚未接入；云资源管理、账单明细、百炼专属模型额度不在本轮实现范围。

## 0.2.1 — 2026-09-24

类型：按用户指定版本号登记的多平台功能版本。状态：测试阶段，尚未正式发布。客户端配置：`0.2.1+3`。

### 新增

- Android 新增统一“API 账户”入口，可添加、编辑和移除多个账户；同一服务商可配置多个 Key。
- 新增 OpenRouter 账户余额适配，使用 Management Key 查询累计购买额度、已用额度和剩余额度。
- 新增 OneAPI 兼容货币账单适配，支持配置站点地址和币种；明确区分站点返回的账户额度与令牌额度。
- 新增自定义余额接口，支持 HTTPS GET、Bearer Key、JSON 余额字段路径和币种配置。
- 新增桌面组件账户选择，可指定组件显示的账户。

### 调整

- 将原有 DeepSeek 单账户配置扩展为统一账户管理，保留 DeepSeek 多币种可用、赠送和充值余额查询能力。
- Android 首页仅显示已添加的 API 账户，移除订阅和阿里云演示卡片；组件预览及空状态改为通用 API 账户文案。
- 各账户独立处理刷新失败与过期余额；不同币种不合计。
- 服务商目录按用户优先级排列：DeepSeek、硅基流动、OpenRouter、阿里云百炼、OneAPI 兼容接口、智谱/Kimi、OpenAI。
- 保留读取旧版加密 DeepSeek 凭据的兼容路径；账户设置变更后写入新的加密账户库。

### 修复与保护

- 接口明确返回失败时，不再将响应中的数字当作有效余额；缺失余额不按零处理。
- OneAPI 使用量按接口单位转换；无法确认货币计费时不把 Token 额度当作钱，无限额度显示余额未知。
- 更换服务商或自定义地址必须重新输入 Key；自定义站点查询前显示目标地址供确认，禁止跟随重定向。
- 账户保存失败保留原配置；账户库读取失败时阻止覆盖保存。
- 修正账户编辑中的空值检查警告，并限制组件标题为单行，避免长名称挤占余额区域。

### 项目约定

- 当前版本按用户要求登记为 0.2.1；建立补丁、功能、大幅 UI 修改或模块调整的版本递增规则。
- 每次更新必须同步维护本日志，记录版本号、日期、变更类型、具体内容、验证结果及已知限制。

### 验证与已知限制

- 功能提交 `9e917da` 已通过 30 项 Flutter 测试、17 项协议及服务端测试，以及 Web、Android、Windows、macOS、iOS 模拟器构建。
- 上述已验证安装包为 `0.2.0+2`；登记后的源码版本为 `0.2.1+3`，旧安装包编号不会被追溯修改。
- 硅基流动、阿里云百炼、智谱、Kimi、OpenAI 尚未完成余额适配；目录中标注待适配，不收集 Key、不以模拟数据冒充余额。
- 真实平台凭据、加密存储升级、桌面组件及设备重启行为仍需 Android 真机验收。
- 暂无后台定时刷新；自定义接口暂不支持 Cookie、签名认证、POST、任意请求头或公式。

版本规则见 [版本编号规则](docs/VERSIONING.md)，平台依据见 [平台接入说明](docs/PROVIDERS.md)。
